# For local development - no authentication required
backend:
  name: git-gateway
local_backend: true

# For production - requires GitHub authentication
# backend:
#   name: github
#   repo: Ricky-the-Hutt/Code-Vibing-website
#   branch: main
#   base_url: https://www.rickylopes.pt
#   auth_endpoint: api/auth

media_folder: "public/images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "blog"
    label: "Blog"
    folder: "content/blog"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: "Layout", name: "layout", widget: "hidden", default: "blog"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Date", name: "date", widget: "datetime"}
      - {label: "Excerpt", name: "excerpt", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}

  - name: "pages"
    label: "Pages"
    files:
      - label: "Home Page"
        name: "home"
        file: "content/pages/home.md"
        fields:
          - {label: "Name", name: "name", widget: "string"}
          - {label: "Description (Control)", name: "description_control", widget: "text"}
          - {label: "Description (Vibe Focus)", name: "description_vibe", widget: "text"}

      - label: "About Page"
        name: "about"
        file: "content/pages/about.md"
        fields:
          - {label: "Title", name: "title", widget: "string"}
          - {label: "Body", name: "body", widget: "markdown"}
